name: CD

on:
  release:
    types:
      - published

jobs:
  cd:
    uses: halo-sigs/reusable-workflows/.github/workflows/theme-cd.yaml@v3
    permissions:
      contents: write
    secrets:
      halo-pat: ${{ secrets.HALO_PAT }}
    with:
      app-id: app-5zrn9isy
      node-version: 22
      pnpm-version: 9
